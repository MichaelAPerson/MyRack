{
  "name": "myrack",
  "private": true,
  "version": "0.2.0",
  "description": "MyRack - self-hosted dashboard for monitoring Linux/Windows/macOS devices",
  "scripts": {
    "setup": "npm install --prefix hub && npm install --prefix agent && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node start.js",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
